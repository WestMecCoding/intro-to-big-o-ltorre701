// intiliaze a variable called someStr to some string of your choice
let someStr = "some string of your choice"
// create a named function that takes an argument `str`
function reverseString(str) {
    // in the function reverse `str`
    str = str.split('').reverse().join('');
    // return `str`
    return reverse;
}
// outside of the function Log the function and pass someStr
console.log(someStr(someStr));